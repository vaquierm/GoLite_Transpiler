package test
func main() {
	var i int
	var f float32
	a := i + i
	b := f + f
	c := "Hello" + "World"
	d := i + 1
	e := f + 1.2
	ff := i / i
	g := f / f
	h := true && false
	ii := true || false
	j := i < 1
	k := i != i
	l := i == i
}